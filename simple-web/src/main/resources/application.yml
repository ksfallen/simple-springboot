spring:
  thymeleaf:
    cache: false

#  datasource:
#    driver-class-name: com.mysql.jdbc.Driver
#    username: root
#    password: root
#    url: jdbc:mysql://127.0.0.1:3306/simple?useUnicode=true&characterEncoding=utf-8&useSSL=false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  security:
    enabled: false

mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml

mapper:
    mappers:
        - com.simple.common.BaseMapper
    not-empty: false
    identity: MYSQL
    style: normal
